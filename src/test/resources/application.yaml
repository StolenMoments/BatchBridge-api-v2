spring:
  datasource:
    url: jdbc:h2:mem:batchbridge;MODE=MariaDB;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

batch-bridge:
  cors:
    allowed-origins: http://localhost:5173
  swagger:
    contact:
      name: test
      email: test@example.com
  api-keys:
    claude: test
    grok: test
    gemini: test
external-context:
  github-token:
  atlassian-base-url:
  atlassian-email:
  atlassian-api-token:
